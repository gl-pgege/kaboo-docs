[project]
name = "pipeline-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["kaboo-workflows[openai]"]

[tool.uv.sources]
kaboo-workflows = { path = "../../kaboo-workflows", editable = true }
